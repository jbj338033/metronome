{
  "name": "metronome",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm -F @metronome/server dev\" \"pnpm -F @metronome/web dev\"",
    "build": "pnpm -r build",
    "start": "pnpm -F @metronome/server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
